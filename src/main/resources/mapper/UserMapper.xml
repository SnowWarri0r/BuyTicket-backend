<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snowwarrior.huikuan.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.snowwarrior.huikuan.model.User">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="balance" jdbcType="DOUBLE" property="balance"/>
    </resultMap>
    <sql id="BaseColumn">
        id,username,`password`,balance
    </sql>
    <select id="getUserById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select
        <include refid="BaseColumn"/>
        from user
        where id = #{id,jdbcType=BIGINT}
    </select>
    <select id="getAllUsers" resultMap="BaseResultMap">
        select
        <include refid="BaseColumn"/>
        from user
    </select>
    <insert id="addUser" keyColumn="id" keyProperty="id" parameterType="com.snowwarrior.huikuan.model.User"
            useGeneratedKeys="true">
        insert into user (username, `password`, balance)
        values (#{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, #{balance,jdbcType=DOUBLE})
    </insert>
    <update id="updateUserById" parameterType="com.snowwarrior.huikuan.model.User">
    </update>
</mapper>